/*global beforeEach, describe, it, assert, expect  */
'use strict';

describe('Publisher Model', function () {

    beforeEach(function () {
        this.PublisherModel = new Backbone.Models.Publisher();
    });

});
